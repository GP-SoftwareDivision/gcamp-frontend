/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com', 'images.unsplash.com', 'www.goldenplanet.co.kr'],
  },
}

export default nextConfig
